"use client";

export default function {{pascalCase feature}}{{pascalCase page}}Page() {
  return (
    <div data-testid="{{dashCase feature}}-{{dashCase page}}-page">
      {{pascalCase feature}} {{pascalCase page}} Page
    </div>
  );
}
